services:
  - type: web
    name: genebridge
    runtime: python
    buildCommand: pip install -r requirements.txt && python -c "import genebridge_backend"
    startCommand: gunicorn genebridge_backend:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
